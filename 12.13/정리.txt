변수의 범위
자바스크입트에서는 객체, 함수 모두 변수(상수)

1. 지역변수
함수 내에서 선언된 변수를 가리킵니다.

2. 변역변수

객체(Object)

프로퍼티(Property)

배열과 차이범

객체를 만드는 방법
1.리터럴 표기법
2.생서자를 이용한 객체생성(여러개만듬)
3.클래스를 이용한 객체생성(설계도개념)
4. 